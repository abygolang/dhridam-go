# export PATH=$PATH:/usr/local/go/bin
go run ./cmd/web -addr=":9000"
# go run .
# go run main.go
# go run www.dhridam.com

# go run is convinient way to try out code. It's a shortcut that compiles the code, 
# creates an executable binary in /tmp directory, and then runs binary in 1 step.